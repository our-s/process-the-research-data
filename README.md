# process-the-research-data

**Programming Exercise 1:** Processing Research Data - Origem/Destino

* In your Linux console, you should run the following command.

  Command-line: `$ python local.py`

  Run in the virtual environment -> see a prefix `(myvenv)` in your console: `(myvenv) … $ python local.py`
